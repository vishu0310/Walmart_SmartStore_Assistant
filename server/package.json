{
  "name": "server",
  "version": "1.0.0",
  "description": "Create a `.env` file in the `server` directory with the following content:",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "seed": "node seedData.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "mongoose": "^8.16.0",
    "openai": "^5.7.0"
  }
}
